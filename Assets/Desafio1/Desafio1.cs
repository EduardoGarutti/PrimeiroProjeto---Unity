using UnityEngine;

public class Desafio1 : MonoBehaviour
{
    public int x, y;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("A soma entre " + x + " + " + y + " é igual a: " + (x + y));    
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
